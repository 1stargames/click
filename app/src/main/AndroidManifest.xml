<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="games.onestar.click">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data
            android:name="activity_key"
            android:value="SPEED_CLICKING" />

        <!-- activities -->
        <activity android:name="games.onestar.splashscreen.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="games.onestar.click.ui.activities.MainActivity" />
        <activity
            android:name="games.onestar.click.ui.activities.PlayActivity"
            android:configChanges="orientation|screenSize" />
        <activity android:name="games.onestar.click.ui.activities.TimeActivity" />

        <!-- receivers -->
        <receiver android:name=".receivers.SplashScreenReceiver">
            <intent-filter>
                <action android:name="games.onestar.START_SPEED_CLICKING" />
            </intent-filter>
        </receiver>

        <!-- services -->
    </application>

</manifest>